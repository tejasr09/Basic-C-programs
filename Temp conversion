#include <stdio.h>

int main() {
    float f, celcius;
    printf("Enter the temperature in farenheit:");
    scanf("%f", &f);
    
        celcius=(f-32)*5/9;
        
    printf("The temperature in celcius is %f", celcius);
    return 0;
}
