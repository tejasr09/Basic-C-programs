#include <stdio.h>

int main() {
   int num, mul, i;
   printf("Enter the number of which multiples are needed:");
   scanf("%d", &num);
for(int i=1;i<=10;i++){
    mul=num*i;
    printf("%dX%d=%d\n", num, i, mul);
}
    return 0;
}
