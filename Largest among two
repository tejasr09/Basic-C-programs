#include <stdio.h>

int main() {
    int a,b;
    printf("Enter the two numbers:");
    scanf("%d %d", &a, &b);
if(a>b)
    printf("%d is largest among the two.", a);
else
    printf("%d is the largest among the two.", b);
    return 0;
}
