#include <stdio.h>

int main() {
    int fact;
    printf("Enter the number:");
    scanf("%d", &fact);
for(int i=fact-1;i>0;i--)
{
    fact=fact*i;
}
printf("The factorial of the number is:%d",fact);
    return 0;
}
