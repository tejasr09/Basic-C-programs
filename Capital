#include <stdio.h>
int main(){
    char ch;
    printf("Enter the letter to be identified:");
    scanf("%c", &ch);
    if (ch>='A' && ch<='Z')
    {
        printf("It is a capital letter");
    }
    else
    {
        printf("It is not a capital letter");
    }

    return 0;
}
